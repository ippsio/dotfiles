#!/bin/bash
readonly REPO=$(git_reponame)
AUTHZ_TOKEN=$(cat ~/.config/hub| grep "oauth_token:"| awk '{ print $2 }')
curl -X GET -s -H "Authorization: token ${AUTHZ_TOKEN}" https://api.github.com/repos/${REPO}/pulls/$1

