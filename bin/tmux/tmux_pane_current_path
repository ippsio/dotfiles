#!/usr/bin/env bash
if [[ "$2" == "--current-window" ]]; then
  tmux display-message -t "$1" -p "#{pane_current_path}" | sed -E "s|${HOME}|~|"
else
  tmux display-message -t "$1" -p "#{pane_current_path}" | sed -E "s|^.*/||"
fi
