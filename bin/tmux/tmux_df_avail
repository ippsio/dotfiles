#!/usr/bin/env bash

client_width=$(~/dotfiles/bin/tmux/tmux_client_width)
if [[ ${client_width} -gt 160 ]]; then
  df -k /| awk '{ if (NR==2) { printf "F%.1fG|U%.1fG", $4*1024/10^9, ($2-$4)*1024/10^9 }}'
else
  df -k /| awk '{ if (NR==2) { printf "F%.1fG", $4*1024/10^9 }}'
fi
