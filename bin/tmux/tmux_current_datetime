#!/usr/bin/env bash
client_width=$(~/dotfiles/bin/tmux/tmux_client_width)
if [[ ${client_width} -gt 160 ]]; then
  env LC_ALL=C date +'%a,%m/%d %H:%M:%S'
elif [[ ${client_width} -gt 100 ]]; then
  env LC_ALL=C date +'%H:%M:%S'
fi
