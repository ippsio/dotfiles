#!/usr/bin/env bash
h="$1"
get_pr_no() {
  git_pr_no_by_hash "${h}"| sed -E 's/[[:blank:]]*//'| sed -E 's/#//'
}

pr_no=$(get_pr_no)
if [[ -n "${pr_no}" ]]; then
  open "https://github.com/$(git_reponame)/pull/${pr_no}"
else
  echo "No PR found."
fi

