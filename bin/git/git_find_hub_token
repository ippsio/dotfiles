#!/usr/bin/env bash
if [[ -n "${HUB_TOKEN}" ]]; then
  printf "%s" "${HUB_TOKEN}"

elif [[ -e ~/.config/hub ]]; then
  outh_token=$(cat ~/.config/hub| grep "oauth_token:"| awk '{ print $2 }' 2>/dev/null)
  if [[ -n "${outh_token}" ]]; then
    printf "%s" "${outh_token}"
  fi
fi
