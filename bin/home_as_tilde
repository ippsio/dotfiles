#!/bin/bash

# NOTE: Why realpath?
# Because if "/Users/username/dotfiles/bin/../" was given,
#  - it's     good to return "~/dotfiles/".
#  - it's not good to return "~/dotfiles/bin/../".

result=$(echo "$1" \
  | xargs realpath \
  | sed "s#^${HOME}#~#"
)
echo "${result}"
